import React, { ReactElement } from 'react'

interface Props {
    
}

export default function EditPageNews({}: Props): ReactElement {
    return (
        <div>
            EditPageNews
        </div>
    )
}
