import React, { ReactElement } from 'react'

interface Props {
    
}

export default function AboutPage({}: Props): ReactElement {
    return (
        <div>
            AboutPage
        </div>
    )
}
