import React, { ReactElement } from 'react'

interface Props {
    
}

export default function CommentsPage({}: Props): ReactElement {
    return (
        <div>
            CommentsPage
        </div>
    )
}
